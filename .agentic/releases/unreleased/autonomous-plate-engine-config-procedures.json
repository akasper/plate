{
  "slug": "autonomous-plate-engine-config-procedures",
  "change_type": "process",
  "surface": "src/plate_core/autonomy.py (skeleton + dataclasses AutonomyStatus/ProjectSnapshot/CycleReport/ProcedureDef + AutonomyEngine __init__/get_status/introspect basic), src/plate_core/mcp_server.py (basic plate_autonomy_status + plate_autonomy_run_cycle wiring), src/plate_core/plate_config.py (autonomy in ALLOWED/DEFAULT + deep validate + v1.2 + migration), .plate (explicit autonomy section), docs/design/autonomous-plate-engine.md (ref), docs/research/budget-governor-models-*.md (update), AGENTS.md (notes), .agentic/releases/unreleased/ fragment (this slice)",
  "summary": "AutonomyEngine skeleton (core dataclasses + basic AutonomyEngine for Epic #470) + initial plate_autonomy_* MCP surfaces (status, run_cycle) + plate_config autonomy schema addition (v1.2, deep validate for risk/token_budget/loop/bools/enums, migration, extension keys, DEFAULT) + explicit .plate example + research/design/AGENTS alignment. This is the engine skeleton + config/MCP groundwork slice. Full governor, procedures, CLI, observability, persona updates, and AUTONOMOUS_MODE transition in sibling PRs.",
  "migration_impact": "Repos get AutonomyEngine skeleton and basic MCP status/run_cycle. .plate autonomy section now validated at v1.2 (add it for explicit control; risk_tolerance 'off' disables new auto per matrix). Migration from 1.1 adds the section via deep-merge defaults. Later slices will wire more surfaces and procedures.",
  "agent_notes": "Basic autonomy surfaces (plate_autonomy_status, run_cycle) now available via MCP/CLI. Use the skeleton for introspect/decide stubs. Full engine behavior (enforce, tick, procedures) lands in follow-ups. Respect .plate autonomy for any autonomous work. Quiet ops apply.",
  "links": [],
  "breaking": false,
  "migration_guidance": [
    "Add the 'autonomy' section to .plate (or run upgrade) for v1.2 schema; set risk_tolerance explicitly ('medium' balanced default; 'off' for no new autonomous per Epic risk matrix).",
    "The skeleton enables basic status/introspect via MCP; full run_cycle/procedures in sibling changes.",
    "For legacy .github/AUTONOMOUS_MODE users: this is incremental; full transition (including template removal) in later Epic slices."
  ]
}